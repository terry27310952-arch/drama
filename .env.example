PORT=4000

# Required for real LLM generation.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5

# Optional. Without these, the app uses local JSON storage in .data/.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=shortdrama-assets
